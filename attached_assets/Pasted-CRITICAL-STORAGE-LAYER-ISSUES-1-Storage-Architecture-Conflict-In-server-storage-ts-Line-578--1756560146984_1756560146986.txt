CRITICAL STORAGE LAYER ISSUES
1. Storage Architecture Conflict
In server/storage.ts:

Line 578: export const storage = new DatabaseStorage();
But the file shows MemStorage class implementation with hardcoded data
This creates unpredictable behavior depending on which storage actually runs
2. Admin Authentication Password Format Bug
In MemStorage.getAdminByUsername():

password: '8bc11c416f6eb49929d72b38194ba53ba25157ee75cb280fc25d30ffe8a991f7:32cd38562c10c43695ffdd552ff8399d61cf54c85eaab9445043b77a2ce77c35ac8fb6809fddc08dc4cf89ebdba06d7578382452fe3032e74b74d7ae8de5c9f8'
Uses COLON format (hash:salt)
But in auth.ts line 18:

return `${buf.toString("hex")}.${salt}`;  // Creates DOT format
And in routes.ts comparePasswords():

Has buggy logic trying to handle both formats but fails on buffer length mismatches
API ROUTING CONFLICTS
3. Duplicate Route Definitions
In server/routes.ts around lines 400-450:

// FIRST /api/tasks endpoint
app.get("/api/tasks", async (_req: Request, res: Response) => {
  // Returns all tasks
});
// SECOND /api/tasks endpoint - OVERWRITES THE FIRST
app.get("/api/tasks", async (req: Request, res: Response) => {
  // Filters by userId
});
The second definition overwrites the first, causing inconsistent behavior.

DATA PERSISTENCE PROBLEMS
4. Task Count Mismatch Logic
Dashboard endpoint /api/dashboard/${userId} (line ~300):

available: tasks.filter((t: any) => !completed.some((c: any) => c.taskId === t.id)).length
Tasks endpoint /api/tasks (line ~400):

task.isActive && !completedSet.has(task.id)
Different filtering logic causes dashboard to show "6 available" while tasks page shows "0"
5. Sample Data Initialization Issues
In initializeSampleTasks() function:

Creates admin with username "administrator" (not "admin")
If using MemStorage, data isn't persisted between restarts
If using DatabaseStorage, initialization might fail silently
WEBVIEW CONSOLE ERRORS
6. WebSocket Connection Issues
From console logs:

[vite] connecting...
{"type":"unhandledrejection"}
Continuous HMR connection failures
Promise rejections in WebSocket connections
Affects real-time task notifications
7. React Lifecycle Warnings
Warning: Using UNSAFE_componentWillMount in strict mode is not recommended
Third-party components using deprecated React patterns
Could cause unexpected renders and state inconsistencies
AUTHENTICATION SYSTEM CONFLICTS
8. Multiple Auth Mechanisms
In routes.ts isAdminAuthenticated() function:

const sessionAdmin = req.session?.admin?.id;
const regularAdmin = req.isAuthenticated() && req.user;
return !!(sessionAdmin || regularAdmin);
Three different authentication checks create confusion
Session vs Passport.js conflicts
9. Admin Login Failure Sequence
User enters "admin" / "Admin@123!"
getAdminByUsername("admin") returns hardcoded admin with COLON format password
comparePasswords() tries to hash input with wrong format
Buffer length mismatch in timingSafeEqual()
Login fails with "Invalid admin credentials"
DATABASE INTEGRATION PROBLEMS
10. Storage Implementation Mismatch
Key Issue: Your app shows DatabaseStorage in imports but MemStorage implementation:

Admin operations expect database tables but get in-memory maps
Task operations may hit database while user operations hit memory
Inconsistent data between different app components
11. Promotion Requests Data Flow
Console shows promotion requests exist in database:

GET /api/promotion/requests 200 in 144ms :: [{"id":1,"userId":2,"planId":3...
But admin can't access due to authentication failures

PERFORMANCE AND STABILITY ISSUES
12. Cache Inconsistencies
Dashboard shows cached task counts
Tasks page shows real-time database queries
No cache invalidation strategy
13. Error Handling Gaps
Multiple try/catch blocks but inconsistent error responses
Some endpoints return 500 for validation errors
Client-side error boundaries not comprehensive
CRITICAL IMPACT SUMMARY
Your application has cascading failures:

Admin can't login → Can't manage promotion requests
Storage layer conflicts → Unpredictable data persistence
Task display inconsistency → Users see wrong task counts
WebSocket failures → No real-time updates
Authentication confusion → Security gaps and access issues
ROOT CAUSE
The fundamental issue is architectural inconsistency - your app is trying to use both in-memory storage AND database storage simultaneously, with mismatched authentication systems and conflicting data access patterns.

The system needs a complete storage layer unification and authentication system consolidation to function properly.

This explains why dashboard shows 6 tasks but tasks page shows 0, why admin login fails, and why promotion requests aren't accessible despite being in the database.